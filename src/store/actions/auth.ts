import { AllDispatchProp, Ilogin, Iregister } from "../types";

export const login = (data: Ilogin) => {
  return async (dispatch: AllDispatchProp) => {};
};

export const register = (data: Iregister) => {
  return async (dispatch: AllDispatchProp) => {};
};
